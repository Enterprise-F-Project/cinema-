spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: WARN
    com.cinema: INFO
    org.hibernate.SQL: WARN

management:
  endpoint:
    health:
      show-details: never
